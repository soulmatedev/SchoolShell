<Page x:Class="AdminPanel.AppPages.TeacherPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AdminPanel.AppPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TeacherPage">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock FontSize="18" FontWeight="Bold" Opacity="0.8" Text="Учителя"  Grid.Column="0"/>
            <Button Content="Выйти" Grid.Column="1" Width="100" HorizontalAlignment="Right"  Background="LightSkyBlue" Click="Button_GoBack"/>
        </Grid>
        </StackPanel>

        <StackPanel Margin="10" VerticalAlignment="Bottom">
            <Button Content="Добавить" Margin="4" Background="LightSkyBlue"/>
            <Button Content="Удалить" Margin="4" Background="LightSkyBlue"/>
            <Button Content="Изменить" Margin="4" Background="LightSkyBlue"/>
        </StackPanel>
       
    </Grid>
</Page>
